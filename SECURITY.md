# 安全政策

## 支持版本

本项目为纯静态站点 + 维护脚本，无对外服务。安全问题修复仅针对 `main` 分支的最新版本；历史版本不提供补丁。

| 版本 | 是否支持 |
|------|---------|
| 1.3.x | ✅ |
| 1.2.x | ✅ |
| < 1.2 | ❌ |

## 报告漏洞

**请勿通过公开 Issue 报告安全漏洞。** 请通过以下任一私密渠道提交：

1. GitHub 私有安全公告（推荐）：仓库 → *Security* → *Report a vulnerability*
2. 若你无法使用私有安全公告（例如没有仓库写权限）：请开一个**不含任何细节**的 Issue，只说明"有安全问题希望私下沟通"，维护者会与你约定加密渠道

> 说明：GitHub 已取消站内私信功能，仓库主页不是私密渠道，请勿在其中描述漏洞细节。

请在报告中尽量包含：

- 问题类型（如 XSS、依赖漏洞、脚本任意文件写入等）与受影响文件路径
- 复现步骤或最小复现样例
- 影响范围与潜在危害
- 如有可行，附上修复建议

## 响应时效

- **确认收到**：3 个工作日内
- **初步评估**：7 个工作日内给出是否受理及严重级别
- **修复发布**：确认后视严重级别尽快修复，并同步更新 `CHANGELOG.md`

## 本项目的安全边界

需要说明的是：

- 站点为纯前端静态页面，不收集、不上传任何用户数据，不涉及用户凭证或密钥存储
- `scripts/` 下的脚本需要联网抓取第三方政策页面，**请仅运行仓库内的脚本，并审阅其网络请求目标**；不要在运行环境中放置与本项目无关的生产密钥
- CI 工作流使用的 `OPENAI_API_KEY` 等凭据存放于 GitHub Secrets，脚本不得将其写入日志或产物文件

如发现脚本会写出敏感信息、或工作流存在凭据泄露风险，同样按上述渠道报告。
